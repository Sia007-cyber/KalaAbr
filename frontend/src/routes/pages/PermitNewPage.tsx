/* =============================================================
   PermitNewPage — route wrapper for the two-step issuance form.
   ============================================================= */

import { PermitForm } from '../../components/permits/PermitForm'

export function PermitNewPage() {
  return <PermitForm />
}